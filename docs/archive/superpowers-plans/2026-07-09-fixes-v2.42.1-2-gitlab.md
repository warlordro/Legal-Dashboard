# Fixuri v2.42.1 → v2.42.2 pe GitLab (branch existent) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aplica pe clona GitLab (aflata la v2.42.0) forma FINALA a fixurilor v2.42.1–v2.42.2 din raportul post-merge GitHub — bugurile web 1a, 2, 3, 4, 5, 6, 7 plus fixurile desktop 8, 9, 10 — direct pe branch-ul existent `feat/v2.42.0-users-settings`, fara branch nou. Plus adaugirile decise pe parcurs: mitigarea F06 din auditul extern (Task 8b), bugul mailto din importul de utilizatori (Task 8c), migrarea modelelor OpenAI la familia GPT-5.6 (Task 8d), extinderea changelog-ului v2.42.0 (Task 8e), audit advers cu review-panel inainte de commit-ul unic (Task 9) si raportul HTML final (Task 10).

**Architecture:** Fixuri chirurgicale, un commit per bug (sau grup coerent). Se aplica DOAR forma finala v2.42.2 — NU varianta intermediara v2.42.1 a body limit-ului (Bug 1b: bodyLimit global montat naiv inaintea routerelor umbrea limitele per-ruta 25MB/10MB/15MB). Testele de ordine a middleware-ului ruleaza prin app-ul REAL bootat din `index.ts` (pattern `index.test.ts`: `importFreshIndex` + port real + fetch HTTP) — testele izolate per-router sunt structural incapabile sa prinda buguri de mount order (exact asa a trecut Bug 1 de 1706 teste pe GitHub). Testele de semantica interna de middleware (rate-limit) raman in `rate-limit.test.ts` cu app-uri Hono minimale — semantica `c.finalized` e a runtime-ului Hono, nu a ordinii de mount.

**Tech Stack:** Node 22 + Hono ^4.12 + better-sqlite3 (backend), Electron 41 (`electron/main.js`), Vitest, Biome.

## Global Constraints

- **Branch: `feat/v2.42.0-users-settings`** — decizia userului: lucram DOAR pe acest branch, NU branch nou, **NIMIC pe main** (FOARTE IMPORTANT: main e urmarit de deployment-ul Dokploy — un push pe main = deploy pe productie).
- **ZERO lucru pe infra**: nu se configureaza Caddy / reverse-proxy / Dokploy — strict cod pe branch (regula userului, 2026-07-09).
- **UN SINGUR COMMIT LA FINAL (regula userului, 2026-07-09):** pasii "Gates + stage" din taskuri fac DOAR `git add` (staging incremental) — NICIUN `git commit` per task. Commit-ul unic se face in Task 9, dupa gate-ul complet, cu mesajul compus din notele per task. Gate-urile per task (biome + tsc + testele fisierelor atinse) raman obligatorii — doar commit-ul se amana.
- **Push pe GitLab DOAR la final**, dupa Task 9 (gate complet + smoke + commit-ul unic), cu confirmarea userului — branch-ul e sub review si push-ul actualizeaza MR-ul.
- NU reproduce varianta intermediara v2.42.1 a body limit-ului (Bug 1b). Ordinea de mount din `index.ts` e load-bearing: `preAuthRateLimit` → `ownerContext` → (web: patUsageAudit, patSecurity) → `rateLimit` → (web: openapi, patCapabilityGate) → `originGuard` → **globalBodyLimit cu exceptii** → (web: apiTokensRouter) → restul routerelor.
- Envelope standard `{ data, error: { code, message }, requestId }`; `fail()`/`ErrorCodes` din `backend/src/util/envelope.ts`.
- Romana fara diacritice in cod sursa si mesaje.
- Schimbari chirurgicale — nu "imbunatati" cod adiacent; zero features in plus.
- **Nu sincroniza infra** intre repo-uri (regula sync v2.42.0): doar surse + dev-scripts; Dockerfile/deploy/workflows/smoke-deploy/deps raman per-repo.
- **FARA bump de versiune in acest plan** — branch-ul e in review ca v2.42.0; bump-ul pe GitLab (si CHANGELOG) se decide separat la release. Daca se face bump ulterior: `npm version` rescrie package.json multi-line si biome il respinge — ruleaza `npx biome check --write package.json` INAINTE de tag (capcana invatata pe GitHub la v2.42.2).
- Numerele de linie citate sunt de la data planului — ancoreaza edit-urile pe continutul snippet-urilor, nu pe numere.
- Dupa teste Node care ating `better-sqlite3`: `npm run rebuild:electron` la final de sesiune daca urmeaza smoke Electron.

## Decizii luate (asumptii explicite)

1. **Toate fixurile pe branch-ul existent** — cerinta explicita a userului (branch-ul e in asteptare la reviewer; nu se trimite branch nou). Reviewer-ul va vedea commit-urile noi in acelasi MR.
2. **Fixurile desktop (8–10) SE aplica si pe GitLab** — `electron/main.js` si `backend/src/index.ts` sunt surse, iar regula de sync intre repo-uri e "doar surse"; paritatea cu GitHub evita drift chiar daca GitLab nu face azi build-uri desktop.
3. **Starea verificata in cod (2026-07-09)**: toate cele 10 buguri exista pe branch in forma v2.42.0 — `apiTokens.ts:64` face `c.req.json()` fara limita si nu exista niciun `bodyLimit` in `index.ts`; release-ul preAuth nu e in `finally` (`rate-limit.ts:190-199`); weight doar pe `/api/ai/analyze-multi` (`rate-limit.ts:90`); fereastra proaspata fara plafon (`rate-limit.ts:62-63, 92-93`); `/health/detail` doar cu gate de loopback (`index.ts:307-347`); 9 functii cu `ownerId = "local"` default (7 in `avizRepository.ts`, 2 in `searchRepository.ts`); `finalize()` cu return ignorat (`scheduler.ts:548`) si `durationMs: 0` in catch (`scheduler.ts:317`); `IS_DEV` din `NODE_ENV` (`main.js:122`), IPC fara validare sender (`main.js:276-300`), shutdown 5s (`main.js:83`), health-check fara nonce (`main.js:242-254`).
4. **Testele web pentru tokens 413 folosesc sesiune JWT reala**: user inserat direct cu `insertUser()` (pattern deja folosit in `index.test.ts:227` pentru `createApiToken`) + `signAuthToken()` din `auth/jwt.ts` cu `jti` (post-v2.38 semantics, payload identic cu `auth.ts:289-298`). Rol `admin` (managementul de tokenuri e admin-only in web din v2.41).
5. **Cele doua teste existente `/health/detail` web-mode din `index.test.ts` (emailConfigured=false/true) trec pe desktop mode** — telemetria SMTP e identica intre moduri, iar intentia testelor (detectia configului) se pastreaza; web mode primeste test nou de 403.
6. **Din auditul extern «Bugs si Rezilienta v2.42.2» intra in acest branch DOAR mitigarea minima F06** (Task 8b — catch pe cele 3 `writeSSE` fire-and-forget din rnpm.ts; inchide un vector de proces-kill in web mode cu 3 linii). Restul findings-urilor confirmate merg in follow-up (sectiunea de la finalul planului) — sunt schimbari de contract/arhitectura care ar umfla MR-ul aflat in review si ar diverge de sursele GitHub (fixurile trebuie aplicate o data si sincronizate in ambele repo-uri).
7. **Migrare modele OpenAI: GPT-5.4 → familia GPT-5.6 (Task 8d, decizia userului 2026-07-09).** Mapare pe tier-uri: `gpt-5.4` (Premium) → **5.6 Sol**, `gpt-5.4-mini` (Echilibrat) → **5.6 Terra**, `gpt-5.4-nano` (Rapid) → **5.6 Luna** (scriere oficiala: "Sol", nu "SOL"). Chei interne NOI versionate (`gpt-5.6-sol/-terra/-luna`) — spre deosebire de precedentul `claude-sonnet` (cheie neversionata pastrata), cheile gpt sunt deja versionate; nimic nu persista selectia client-side (state React cu default-uri), iar backend-ul respinge curat cheile necunoscute cu 400 "Model necunoscut." (`ai.ts:705`). Istoricul `ai_usage` pastreaza cheile vechi (display-only, corect). **Slug-uri si pricing VERIFICATE (2026-07-09, trei surse: catalogul live OpenRouter + aipricing.guru + eesel.ai):** model ID-uri native `gpt-5.6-sol` / `gpt-5.6-terra` / `gpt-5.6-luna`; slug-uri OpenRouter `openai/gpt-5.6-sol` / `-terra` / `-luna` (exista si variante `-pro` cu reasoning mode pro — NU le folosim). Pricing per 1M tokens, identic nativ si pe OpenRouter: **Sol $5 in / $30 out, Terra $2.50 in / $15 out, Luna $1 in / $6 out** (preturi de preview — de reverificat la GA; cache read/write nu intra in tabelul nostru). Intrarile 5.4 RAMAN in tabelul de pricing pentru retry-uri in zbor (pattern-ul upgrade-ului Sonnet 5 din `aiUsage.ts:41-45`). **Risc de disponibilitate — DECIS de user (2026-07-09): se face swap-ul direct, fara fallback 5.4 in catalog.** Familia 5.6 e in limited preview pe API-ul nativ OpenAI (GA anuntat "in urmatoarele saptamani" de la 26 iunie 2026), dar tenantul foloseste chei OpenRouter (auto-detect `sk-or-`), iar pe OpenRouter modelele sunt deja live — riscul e irelevant pentru deployment. Caveat documentat: o cheie OpenAI NATIVA configurata inainte de GA fara acces de preview va primi eroare de la provider (mesajul de eroare existent al rutei AI o afiseaza; nu tratam special).
8. **Inchiderea batch-ului (deciziile userului, 2026-07-09):** (a) inainte de commit-ul unic se lanseaza un **audit advers cu review-panel** (`mcp__review-panel__multi_model_review`) pe diff-ul complet — findings confirmate in scope se repara INAINTE de commit, restul merg in follow-up documentat; (b) **changelog-ul din aplicatie + CHANGELOG.md se actualizeaza FARA bump**: se extinde entry-ul v2.42.0 (branch-ul ESTE release-ul v2.42.0 pe GitLab — pattern-ul consolidarii; versiunea afisata ramane consistenta cu package.json) — Task 8e; (c) dupa incheierea completa se genereaza un **raport HTML** (bugs rezolvate → ce efect aveau → ce s-a implementat nou), fisier untracked in radacina, ca celelalte rapoarte — Task 10.
9. **Finding separat (duel review, 2026-07-09) — branch mort `mailto:` in `userImport.ts` — CONFIRMAT si inclus ca Task 8c.** E bug in codul NOU al acestui branch (importul de utilizatori e livrat in sprintul v2.42.0), deci intra natural in MR. Verificat: celulele-hyperlink ExcelJS au intotdeauna forma `{ text, hyperlink }` — ambele campuri prezente — iar `cellToString` verifica `"text" in value` (linia 58) INAINTE de `"hyperlink" in value` (linia 60), deci ramura `mailto:` e de neatins; un email lipit din Outlook cu text afisat diferit de adresa (ex. "Ion Popescu" → `mailto:ion@firma.ro`) importa textul afisat si randul pica la validarea de email. ATENTIE: fixul sugerat in raport ("muta verificarea hyperlink inaintea celei de text") e PREA LARG — `cellToString` ruleaza pe TOATE coloanele, iar un "Nume afisat" cu link ar importa URL-ul/adresa in loc de nume. Fixul corect: prefera hyperlink-ul DOAR cand e `mailto:` si DOAR pe coloana de email (parametru explicit).

## Triaj audit extern «Bugs si Rezilienta v2.42.2» (GPT-5.6 SOL, 2026-07-09)

Verificat finding cu finding in codul de pe branch (2026-07-09). Verdictele:

| # | Finding | Verdict | Relevanta web (GitLab) | Dispozitie |
|---|---------|---------|------------------------|------------|
| F01 | Restore DB concurent cu requesturi active | **REAL — HIGH.** `closeDb()` la `backup.ts:234` e urmat de multe await-uri fs; `getDb()` (`schema.ts:60`) nu are latch de maintenance (doar `shuttingDown`); majoritatea rutelor NU tin `withMaintenanceRead` (doar rnpm, dashboard, aiUsage + servicii). Orice request concurent redeschide DB in plina fereastra de restore. | **MARE** — in web mode traficul concurent multi-user face fereastra realista; restore-ul e expus admin-ului de tenant. | Follow-up P0 (branch separat post-merge). Atentie la design: latch-ul NU poate fi pe tot `withMaintenanceWrite` (daily backup-ul l-ar transforma in 503 global zilnic) — flag setat DOAR in `restoreFromBackupImpl`, verificat in `getDb()`, mapat la 503 in error handler. |
| F02 | Esecuri PortalJust mascate in cautari multi-instanta | **REAL — HIGH (corectitudine).** `dosare.ts:151-162` si `termene.ts:148-159`: `Promise.all` + `.catch(err => { console.error; return []; })` per institutie → 200 cu lista goala, fals negativ juridic. Logat server-side, invizibil client-side. | **MARE** — cautarea e fluxul principal al userilor web; identic si pe desktop. | Follow-up P0 (branch separat): `allSettled` + campuri `partial`/`failedInstitutions` in raspuns + afisare frontend; toate institutiile esuate → eroare, nu 200 gol. Schimbare de contract API — nu intra in MR-ul curent. |
| F03 | SSE poate anunta alerta anulata prin rollback | **REAL, dar fereastra ingusta.** `insertAlert` face `queueMicrotask(notifyNewAlert)` (`monitoringAlertsRepository.ts:276`); tranzactiile better-sqlite3 sunt sincrone, deci microtask-ul ruleaza DUPA tranzactia exterioara — inclusiv dupa rollback (ex. throw intre `applyJobOutcome` si commit in `runOne`). Emailul e DEJA post-commit (pattern `InsertAlertResult`, v2.34 P1-6); SSE nu. | Medie — identic desktop/web (alerts SSE exista in ambele). Probabilitate mica (necesita rollback exact in fereastra). | Follow-up P2: fanout SSE post-commit pe pattern-ul email existent. |
| F04 | Health verde cu scheduler oprit | **REAL, cu doua corectii.** `monitoringScheduler.start().catch(console.error)` la `index.ts:729` e confirmat fire-and-forget; `/health` reflecta doar `ready`, nu scheduler-ul. Corectii: (1) pe GitLab v2.42.0 detaliul ERA vizibil pe loopback in web mode — exact gaura pe care Task 5 o inchide; abia DUPA Task 5 claim-ul auditului devine adevarat pe GitLab; (2) comentariul din `detailedHealthHandler` trimite la `/api/v1/admin/health`, dar **ruta NU exista** (verificat: zero match `health` in `admin.ts`) — comentariu stale, corectat in Task 5. | **MARE dupa Task 5** — web ops ramane fara vizibilitate pe scheduler. | Follow-up P1: endpoint admin-only real (ex. `GET /api/v1/admin/health` cu `getMonitoringSchedulerStatus()` + `emailConfigured`), eventual readiness degraded. Task 5 corecteaza acum doar comentariul stale. |
| F05 | `apiFetch` fara timeout implicit | **REAL.** `frontend/src/lib/api.ts:53-75` — fetch fara deadline cand callerul nu paseaza signal. | Medie-mare — pe web (proxy, retea) hang-urile sunt mai probabile decat pe loopback desktop. | Follow-up P1: `AbortSignal.timeout` implicit compus cu signal-ul callerului + override documentat pentru operatiile legitim lungi (export xlsx, SSE, AI). Atinge un helper central — nu intra in MR-ul curent. |
| F06 | RNPM bulk/split nu asteapta scrierile SSE | **REAL — si MAI GRAV pe web decat sustine auditul.** Trei call site-uri fara catch: `rnpm.ts:498` (`void send(p)` bulk), `:633` (`void stream.writeSSE` started split), `:640` (`void send(p)` split). Doua agravante verificate: (1) pe acest branch `resolveCaptchaKeyForRoute` (rnpmGuards.ts:258) face bulk/split FUNCTIONALE in web mode cand cheile captcha de tenant sunt configurate — exact configuratia deployment-ului GitLab; (2) `process.on("unhandledRejection")` din `index.ts:167-172` face `process.exit(1)` in server mode → un singur writeSSE rejectat (client disconnect mid-bulk) poate dobori TOT procesul web multi-tenant. alerts.ts trateaza peste tot rejection-ul writeSSE ca real — dovada ca failure mode-ul exista. | **CRITICA pentru web** (proces-kill), medie pe desktop (throw → crash dialog). | **Mitigarea minima intra in acest branch (Task 8b)**: catch + log pe cele 3 call site-uri. Coada seriala completa (backpressure, ordering, stop la disconnect) ramane follow-up P1. Fixul se aplica si pe GitHub (paritate surse). |
| F07 | Hook-ul offsite tine maintenance write lock pana la 10 min | **REAL.** `await runOffsiteBackupHook(dest)` ruleaza in `runDailyBackupImpl` sub `withMaintenanceWrite` (`backup.ts:501-555`); `child.kill("SIGKILL")` la timeout omoara doar shell-ul (`cmd.exe /c` / `sh -c`), nu descendentii (scp/rclone). Impact real: blocheaza holderii de `withMaintenanceRead` (scheduler finalize, rnpm saves, dashboard, aiUsage) pe durata upload-ului. | Medie — relevant pe server DOAR cu `LEGAL_DASHBOARD_BACKUP_OFFSITE_CMD` configurat (opt-in). | Follow-up P1: upload dupa eliberarea lock-ului + tree-kill verificabil per platforma. |
| F08 | Lipseste smoke test Electron end-to-end | **REAL (gap).** Zero fisiere e2e in repo. | **Nula pentru web** — strict desktop/packaging. | Backlog P2; mai potrivit pe repo-ul GitHub (acolo se fac build-urile desktop). |
| F09 | Testele acumuleaza listeners de proces | **REAL (mecanism confirmat static).** `index.ts` inregistreaza `unhandledRejection` (167), `SIGTERM` (858), `SIGINT` (861), `beforeExit` (864) la module scope, fara remove; `index.test.ts` re-importa modulul cu `vi.resetModules()` per test → acumulare → `MaxListenersExceededWarning`. Nota: Task 1/5/8 din acest plan ADAUGA boot-uri de index.ts in teste, deci zgomotul creste marginal. | Nula (igiena de teste). | Follow-up P2: teardown cu `process.off` in `__legalDashboardShutdown` sau listeners inregistrati o singura data per proces de test. |

**Concluzie triaj:** 9/9 findings sunt reale ca substanta; corectiile de detaliu sunt la F04 (ruta admin health nu exista — comentariu stale; claim-ul "indisponibil in web" devine adevarat abia dupa Task 5) si F06 (auditul il subevalueaza pentru web: e vector de proces-kill, nu doar backpressure). Pentru deployment-ul web GitLab, ordinea reala de risc e: **F01, F02 (P0, branch-uri separate) → F06 (mitigat ACUM in Task 8b) → F04, F05, F07 (P1) → F03, F09 (P2) → F08 (desktop-only)**.

---

### Task 1: Bug 1a (CRITIC) — plasa globala de body limit pe /api/* cu exceptii exact-match

**Files:**
- Modify: `backend/src/index.ts` (imports + blocul dintre `app.use("/api/*", originGuard)` si mount-ul web `apiTokensRouter`, azi liniile 270–276)
- Test: `backend/src/index.test.ts`

**Interfaces:**
- Consumes: `bodyLimit` din `hono/body-limit`; `fail`, `ErrorCodes` din `./util/envelope.ts`; helpers existente din `index.test.ts` (`importFreshIndex`, `waitForHealth`, `makeTmpDb`, `randomPort`, `SECRET`).
- Produces: middleware `globalBodyLimit` (1MB) pe `/api/*` cu set `LARGE_BODY_ROUTES` exceptat — folosit de testele din acest task si de smoke-ul din Task 9.

- [ ] **Step 1: Scrie testele de regresie (failing) in `index.test.ts`**

Adauga la finalul fisierului (in afara describe-ului PR-9 existent):

```ts
describe("global body limit — Bug 1a (v2.42.2)", () => {
  it(
    "web: POST >1MB pe /api/v1/tokens cu sesiune valida intoarce 413 PAYLOAD_TOO_LARGE",
    { timeout: 25_000 },
    async () => {
      const port = randomPort();
      await importFreshIndex({
        LEGAL_DASHBOARD_PORT: String(port),
        LEGAL_DASHBOARD_DB_PATH: await makeTmpDb(),
        LEGAL_DASHBOARD_AUTH_MODE: "web",
        LEGAL_DASHBOARD_JWT_SECRET: SECRET,
        LEGAL_DASHBOARD_JWT_ISSUER: "legal-dashboard.test",
        LEGAL_DASHBOARD_JWT_AUDIENCE: "legal-dashboard-api",
      });
      await waitForHealth(port);

      const { insertUser } = await import("./db/userRepository.ts");
      const { signAuthToken } = await import("./auth/jwt.ts");
      const { randomUUID } = await import("node:crypto");
      insertUser({ id: "u-bodylimit", email: "bodylimit@test.local", displayName: "Body Limit", role: "admin" });
      const nowSec = Math.floor(Date.now() / 1000);
      const jwt = signAuthToken(
        {
          sub: "u-bodylimit",
          jti: randomUUID(),
          iat: nowSec,
          exp: nowSec + 3600,
          iss: "legal-dashboard.test",
          aud: "legal-dashboard-api",
        },
        SECRET
      );

      const res = await fetch(`http://127.0.0.1:${port}/api/v1/tokens`, {
        method: "POST",
        headers: { "content-type": "application/json", authorization: `Bearer ${jwt}` },
        body: JSON.stringify({ name: "x".repeat(1024 * 1024 + 1024) }),
      });
      expect(res.status).toBe(413);
      expect(((await res.json()) as { error: { code: string } }).error.code).toBe("PAYLOAD_TOO_LARGE");
    }
  );

  it("desktop: POST >1MB pe o ruta inexistenta intoarce 413 (plasa acopera tot /api/*)", { timeout: 20_000 }, async () => {
    const port = randomPort();
    await importFreshIndex({
      LEGAL_DASHBOARD_PORT: String(port),
      LEGAL_DASHBOARD_DB_PATH: await makeTmpDb(),
      LEGAL_DASHBOARD_AUTH_MODE: "desktop",
    });
    await waitForHealth(port);

    const res = await fetch(`http://127.0.0.1:${port}/api/does-not-exist`, {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ pad: "x".repeat(1024 * 1024 + 1024) }),
    });
    expect(res.status).toBe(413);
  });

  it(
    "desktop: POST 1.5MB pe /api/v1/dosare/export.xlsx trece de plasa (validarea rutei, NU 413)",
    { timeout: 20_000 },
    async () => {
      const port = randomPort();
      await importFreshIndex({
        LEGAL_DASHBOARD_PORT: String(port),
        LEGAL_DASHBOARD_DB_PATH: await makeTmpDb(),
        LEGAL_DASHBOARD_AUTH_MODE: "desktop",
      });
      await waitForHealth(port);

      const res = await fetch(`http://127.0.0.1:${port}/api/v1/dosare/export.xlsx`, {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ dosare: [], pad: "x".repeat(Math.floor(1.5 * 1024 * 1024)) }),
      });
      // Limita proprie a rutei e 25MB; 1.5MB trebuie sa ajunga la validarea
      // payload-ului (lista goala => 400), nu la plasa globala (413).
      expect(res.status).toBe(400);
    }
  );
});
```

Nota: daca schema payload-ului de export respinge campuri necunoscute cu alt status, pastreaza assertul esential `expect(res.status).not.toBe(413)` plus statusul real observat — proprietatea testata e ca plasa globala NU taie ruta exceptata.

- [ ] **Step 2: Ruleaza testele noi si confirma ca pica**

Run: `cd backend && npx vitest run src/index.test.ts -t "global body limit"`
Expected: FAIL — testul tokens si testul rutei inexistente primesc alt status decat 413 (plasa nu exista inca); testul export.xlsx trece deja (e regresie pentru Bug 1b).

- [ ] **Step 3: Implementeaza plasa globala in `index.ts`**

Adauga la imports (linga celelalte importuri hono, linia ~7):

```ts
import { bodyLimit } from "hono/body-limit";
```

si (linga importurile util, linia ~54):

```ts
import { ErrorCodes, fail } from "./util/envelope.ts";
```

(Verifica daca `fail`/`ErrorCodes` sunt deja importate in `index.ts`; azi nu sunt.)

Apoi, intre `app.use("/api/*", originGuard);` si blocul `if (getAuthMode() === "web") { app.route("/api/v1/tokens", apiTokensRouter); }`:

```ts
// Bug 1a (v2.42.2): plasa globala 1MB pe /api/*, montata inainte de TOATE
// routerele — POST /api/v1/tokens facea await c.req.json() fara nicio limita
// (orice sesiune autentificata putea bufera sute de MB in procesul partajat).
// Rutele cu payload mare legitim raman guvernate de limitele lor per-ruta
// (25MB export xlsx dosare/termene, 10/15MB name-lists) prin exceptii
// exact-match: bodyLimit din Hono intoarce 413 pe Content-Length fara sa
// apeleze next(), deci plasa montata fara exceptii le-ar umbri (regresia
// v2.42.1 de pe GitHub, Bug 1b — nu o reproduce).
const GLOBAL_BODY_LIMIT = 1024 * 1024;
const LARGE_BODY_ROUTES = new Set([
  "/api/v1/dosare/export.xlsx",
  "/api/v1/termene/export.xlsx",
  "/api/v1/name-lists",
  "/api/v1/name-lists/preview",
  "/api/v1/name-lists/commit",
]);
const globalBodyLimit = bodyLimit({
  maxSize: GLOBAL_BODY_LIMIT,
  onError: (c) => c.json(fail(ErrorCodes.PAYLOAD_TOO_LARGE, "Payload prea mare", c), 413),
});
app.use("/api/*", (c, next) => (LARGE_BODY_ROUTES.has(c.req.path) ? next() : globalBodyLimit(c, next)));
```

- [ ] **Step 4: Ruleaza testele si confirma ca trec**

Run: `cd backend && npx vitest run src/index.test.ts`
Expected: PASS pe toate (inclusiv testele PR-9 existente — plasa nu are voie sa schimbe alt comportament).

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/index.ts backend/src/index.test.ts
npx tsc --noEmit -p backend/tsconfig.json
cd backend && npx vitest run src/index.test.ts && cd ..
git add backend/src/index.ts backend/src/index.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 1a: plasa globala body limit 1MB pe /api/* cu exceptii exact-match
```

---

### Task 2: Bug 2 (MEDIU) — preAuthRateLimit: release corect pe ambele cai de exceptie

**Files:**
- Modify: `backend/src/middleware/rate-limit.ts:190-206` (corpul `preAuthRateLimit` din jurul `await next()`)
- Test: `backend/src/middleware/rate-limit.test.ts`

**Interfaces:**
- Consumes: `preAuthRateLimit`, `resetPreAuthRateLimit` (existente); pattern-ul de mock conninfo/`readClientIp` deja folosit in `rate-limit.test.ts` (reuse setup-ul existent al fisierului — nu crea un al doilea mock paralel).
- Produces: acelasi export `preAuthRateLimit`, cu semantica: release DOAR cand `ownerId` e setat sau raspunsul e 2xx REAL (`c.finalized`); throw pre-auth ramane contorizat.

- [ ] **Step 1: Scrie testele failing**

In `rate-limit.test.ts`, in sectiunea de teste preAuth (cu `resetPreAuthRateLimit()` in beforeEach, conform conventiei fisierului):

```ts
describe("preAuthRateLimit — release pe caile de exceptie (Bug 2 v2.42.2)", () => {
  it("un throw FARA ownerId setat retine tentativa: a 61-a cerere e 429", async () => {
    const app = new Hono();
    app.use("/api/*", preAuthRateLimit);
    app.post("/api/boom", () => {
      throw new Error("boom");
    });
    for (let i = 0; i < 60; i++) {
      const res = await app.request("/api/boom", { method: "POST" });
      expect(res.status).toBe(500);
    }
    const res61 = await app.request("/api/boom", { method: "POST" });
    expect(res61.status).toBe(429);
  });

  it("un throw DUPA ownerId setat elibereaza tentativa: 100 de cereri raman 500, niciodata 429", async () => {
    const app = new Hono();
    app.use("/api/*", preAuthRateLimit);
    app.use("/api/*", async (c, next) => {
      c.set("ownerId", "user-1");
      await next();
    });
    app.post("/api/boom", () => {
      throw new Error("boom");
    });
    for (let i = 0; i < 100; i++) {
      const res = await app.request("/api/boom", { method: "POST" });
      expect(res.status).toBe(500);
    }
  });
});
```

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts -t "Bug 2"`
Expected: FAIL — primul test: pe v2.42.0 un throw sare peste release DAR si peste increment-verificare... concret: release-ul de dupa `await next()` nu ruleaza pe throw, deci primul test ar putea trece; al DOILEA test pica sigur (throw post-ownerId nu elibereaza → a 61-a cerere e 429, nu 500). Confirma minim un test rosu inainte de fix.

- [ ] **Step 3: Implementeaza try/finally cu guard pe c.finalized**

Inlocuieste blocul de la `await next();` pana la finalul functiei (fara sa atingi partea de increment de dinainte):

```ts
  // Bug 2 (v2.42.2): release-ul ruleaza si pe calea de exceptie (finally), dar
  // NUMAI cand autentificarea a reusit. Un caller autentificat care primeste
  // 403/429 sau al carui handler arunca nu trebuie sa consume bucketul IP-only
  // partajat (ar bloca tot traficul din spatele aceluiasi NAT/proxy). Guard-ul
  // pe c.finalized evita capcana getter-ului lazy c.res: pe throw-unwind Hono
  // instantiaza new Response(null) cu status 200, deci un check naiv de status
  // ar elibera exact tentativele NEautentificate esuate pe care limiterul
  // trebuie sa le numere. In timpul unwind-ului c.finalized e false (error
  // handler-ul Hono ruleaza DUPA acest finally).
  try {
    await next();
  } finally {
    if (c.get("ownerId") || (c.finalized && c.res.status >= 200 && c.res.status < 300)) {
      releasePreAuthAttempt(key);
    }
    if (preAuthMap.size > 1000) {
      for (const [k, v] of preAuthMap) {
        if (now > v.resetTime) preAuthMap.delete(k);
      }
    }
  }
```

- [ ] **Step 4: Ruleaza toata suita fisierului**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts`
Expected: PASS (inclusiv testele preAuth existente — semantica pe caile non-exceptie e neschimbata).

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 2: release preAuth in try/finally cu guard pe c.finalized
```

---

### Task 3: Bug 3 (MEDIU) — weight 3x analyze-multi pe ambele mount-uri + pe bucket-ul per-token

**Files:**
- Modify: `backend/src/middleware/rate-limit.ts` (nivel modul + corpul `rateLimit`, azi liniile 54–99)
- Test: `backend/src/middleware/rate-limit.test.ts`

**Interfaces:**
- Consumes: `rateLimit`, `_resetRateLimitForTest` (existente).
- Produces: set modul `ANALYZE_MULTI_PATHS` si variabila `weight` calculata O DATA inainte de bucket-ul per-token — Task 4 se bazeaza pe pozitia acestui `weight`.

- [ ] **Step 1: Scrie testele failing**

```ts
describe("weight 3x analyze-multi — ambele mount-uri + per-token (Bug 3 v2.42.1)", () => {
  it("mount-ul /api/v1/ai/analyze-multi consuma 3 unitati pe bucketul per-owner (a 41-a cerere e 429)", async () => {
    const app = new Hono();
    app.use("/api/*", async (c, next) => {
      c.set("ownerId", "owner-w");
      await next();
    });
    app.use("/api/*", rateLimit);
    app.post("/api/v1/ai/analyze-multi", (c) => c.json({ ok: true }));
    // RATE_LIMIT=120, weight 3 => 40 de cereri incap exact; a 41-a depaseste.
    for (let i = 0; i < 40; i++) {
      const res = await app.request("/api/v1/ai/analyze-multi", { method: "POST" });
      expect(res.status).toBe(200);
    }
    const res41 = await app.request("/api/v1/ai/analyze-multi", { method: "POST" });
    expect(res41.status).toBe(429);
  });

  it("bucketul per-token pondereaza si el: TOKEN_RATE_LIMIT=60 => a 21-a cerere analyze-multi e 429", async () => {
    const app = new Hono();
    app.use("/api/*", async (c, next) => {
      c.set("ownerId", "owner-t");
      c.set("tokenId", "tok-w");
      await next();
    });
    app.use("/api/*", rateLimit);
    app.post("/api/v1/ai/analyze-multi", (c) => c.json({ ok: true }));
    for (let i = 0; i < 20; i++) {
      const res = await app.request("/api/v1/ai/analyze-multi", { method: "POST" });
      expect(res.status).toBe(200);
    }
    const res21 = await app.request("/api/v1/ai/analyze-multi", { method: "POST" });
    expect(res21.status).toBe(429);
  });
});
```

(`_resetRateLimitForTest()` in beforeEach; foloseste mock-ul de IP existent al fisierului.)

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts -t "Bug 3"`
Expected: FAIL — ruta `/api/v1/...` numara 1 azi, deci 429 nu apare la pozitiile asteptate.

- [ ] **Step 3: Implementeaza**

La nivel de modul (sub `TOKEN_RATE_LIMIT`, linia ~25):

```ts
// Bug 3 (v2.42.1): analyze-multi costa 3 apeluri AI; routerul AI e montat dublu
// (/api/ai si /api/v1/ai), deci weight-ul se aplica exact-match pe AMBELE
// path-uri si pe AMBELE bucket-uri (per-token si per-owner).
const ANALYZE_MULTI_PATHS = new Set(["/api/ai/analyze-multi", "/api/v1/ai/analyze-multi"]);
```

In `rateLimit()`, imediat dupa `const now = Date.now();` si INAINTE de blocul per-token:

```ts
  const weight = ANALYZE_MULTI_PATHS.has(c.req.path) ? 3 : 1;
```

In bucket-ul per-token: `{ count: 1, ... }` devine `{ count: weight, ... }` si `tentry.count += 1;` devine `tentry.count += weight;`.

Sterge linia veche `const weight = c.req.path === "/api/ai/analyze-multi" ? 3 : 1;` (era intre bucket-uri); bucket-ul per-owner foloseste `weight`-ul hoistat (codul lui ramane identic).

- [ ] **Step 4: Ruleaza suita**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts`
Expected: PASS.

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 3: weight 3x analyze-multi pe ambele mount-uri si pe bucketul per-token
```

---

### Task 4: Bug 4 (LOW) — plafon si pe fereastra proaspata, ambele bucket-uri

**Files:**
- Modify: `backend/src/middleware/rate-limit.ts` (ramurile de fereastra proaspata din `rateLimit`, azi liniile 62–63 si 92–93)
- Test: `backend/src/middleware/rate-limit.test.ts`

**Interfaces:**
- Consumes: `weight` hoistat din Task 3 (rulat inainte de ambele bucket-uri).
- Produces: acelasi export `rateLimit`; primul request ponderat dintr-o fereastra noua e plafonat.

- [ ] **Step 1: Scrie testul failing**

Testul are nevoie de `TOKEN_RATE_LIMIT=2`, care se citeste la import — foloseste `vi.resetModules()` + import dinamic (module proaspete = maps proaspete, nu mai e nevoie de reset):

```ts
describe("plafon pe fereastra proaspata (Bug 4 v2.42.2)", () => {
  it("TOKEN_RATE_LIMIT=2: primul analyze-multi (weight 3) dintr-o fereastra noua e 429", async () => {
    vi.resetModules();
    const prev = process.env.LEGAL_DASHBOARD_TOKEN_RATE_LIMIT;
    process.env.LEGAL_DASHBOARD_TOKEN_RATE_LIMIT = "2";
    try {
      const { rateLimit: freshRateLimit } = await import("./rate-limit.ts");
      const app = new Hono();
      app.use("/api/*", async (c, next) => {
        c.set("ownerId", "owner-f");
        c.set("tokenId", "tok-f");
        await next();
      });
      app.use("/api/*", freshRateLimit);
      app.post("/api/v1/ai/analyze-multi", (c) => c.json({ ok: true }));
      const res = await app.request("/api/v1/ai/analyze-multi", { method: "POST" });
      expect(res.status).toBe(429);
    } finally {
      if (prev === undefined) {
        // biome-ignore lint/performance/noDelete: env trebuie unset real
        delete process.env.LEGAL_DASHBOARD_TOKEN_RATE_LIMIT;
      } else {
        process.env.LEGAL_DASHBOARD_TOKEN_RATE_LIMIT = prev;
      }
      vi.resetModules();
    }
  });
});
```

Atentie: mock-ul de IP al fisierului trebuie sa ramana activ si pentru modulul re-importat (vi.mock e hoistat per fisier, deci da).

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts -t "Bug 4"`
Expected: FAIL — pe fereastra proaspata count se seteaza fara verificare, primul request intoarce 200.

- [ ] **Step 3: Implementeaza verificarea pe ambele ramuri proaspete**

Bucket per-token:

```ts
    if (!tentry || now > tentry.resetTime) {
      tokenRateLimitMap.set(tkey, { count: weight, resetTime: now + RATE_WINDOW });
      // Bug 4 (v2.42.2): si fereastra proaspata respecta plafonul — cu
      // LEGAL_DASHBOARD_TOKEN_RATE_LIMIT sub 3, primul request ponderat
      // dintr-o fereastra noua scapa altfel neplafonat.
      if (weight > TOKEN_RATE_LIMIT) {
        return fail(c, 429, "rate_limited", "Prea multe cereri pentru acest token.");
      }
    } else {
```

Bucket per-owner (identic, cu `RATE_LIMIT`):

```ts
  if (!entry || now > entry.resetTime) {
    rateLimitMap.set(key, { count: weight, resetTime: now + RATE_WINDOW });
    if (weight > RATE_LIMIT) {
      return fail(c, 429, "rate_limited", "Prea multe cereri. Incercati din nou in cateva momente.");
    }
  } else {
```

- [ ] **Step 4: Ruleaza suita**

Run: `cd backend && npx vitest run src/middleware/rate-limit.test.ts`
Expected: PASS.

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/middleware/rate-limit.ts backend/src/middleware/rate-limit.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 4: plafonul se verifica si pe fereastra proaspata, ambele bucket-uri
```

---

### Task 5: Bug 5 (MEDIU) — /health/detail 403 neconditionat in web mode

**Files:**
- Modify: `backend/src/index.ts` (`detailedHealthHandler`, azi liniile 307–347)
- Modify: `backend/src/index.test.ts` (cele doua teste web-mode `emailConfigured` trec pe desktop; test nou de 403)

**Interfaces:**
- Consumes: `getAuthMode()` (deja importat in `index.ts`).
- Produces: `/health/detail` → 403 `forbidden` in web mode, indiferent de peer; comportamentul desktop/loopback neschimbat.

- [ ] **Step 1: Adapteaza testele existente + scrie testul failing**

In `index.test.ts`:

1. Testul `"/health/detail exposes emailConfigured=false when SMTP_* env vars are missing (Batch 2.3)"` — scoate din env: `LEGAL_DASHBOARD_AUTH_MODE: "web"`, `LEGAL_DASHBOARD_JWT_SECRET`, `LEGAL_DASHBOARD_JWT_ISSUER`, `LEGAL_DASHBOARD_JWT_AUDIENCE`; adauga `LEGAL_DASHBOARD_AUTH_MODE: "desktop"`. Restul testului neschimbat (telemetria SMTP e identica intre moduri).
2. Idem pentru `"/health/detail exposes emailConfigured=true with full SMTP_* config (Batch 2.3)"`.
3. Test nou:

```ts
  it("/health/detail intoarce 403 neconditionat in web mode (Bug 5 v2.42.1)", { timeout: 20_000 }, async () => {
    const port = randomPort();
    await importFreshIndex({
      LEGAL_DASHBOARD_PORT: String(port),
      LEGAL_DASHBOARD_DB_PATH: await makeTmpDb(),
      LEGAL_DASHBOARD_AUTH_MODE: "web",
      LEGAL_DASHBOARD_JWT_SECRET: SECRET,
      LEGAL_DASHBOARD_JWT_ISSUER: "legal-dashboard.test",
      LEGAL_DASHBOARD_JWT_AUDIENCE: "legal-dashboard-api",
    });
    await waitForHealth(port);

    // Apel de pe loopback — exact scenariul reverse proxy same-host care
    // pacalea gate-ul vechi. Web mode = 403 fara telemetrie, indiferent de peer.
    const detail = await fetch(`http://127.0.0.1:${port}/health/detail`);
    expect(detail.status).toBe(403);
    expect(((await detail.json()) as { error: { code: string } }).error.code).toBe("forbidden");
  });
```

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/index.test.ts -t "Bug 5"`
Expected: FAIL — azi loopback-ul primeste 200 cu telemetrie in web mode.

- [ ] **Step 3: Implementeaza gate-ul**

In `detailedHealthHandler`, dupa blocul `if (!ready) {...}` si INAINTE de `if (!isLoopbackPeer(c)) {...}`:

```ts
  // Bug 5 (v2.42.1): in web mode gate-ul de loopback nu e suficient — un
  // reverse proxy pe acelasi host (Caddy, oauth2-proxy) atinge ruta ca
  // "loopback" si ar expune telemetrie operationala (authMode, monitoring,
  // emailConfigured) oricarui client neautentificat. Ops in web mode
  // foloseste /health (liveness 200/503) sau /api/v1/admin/health (auth real).
  if (getAuthMode() === "web") {
    return c.json({ error: { code: "forbidden", message: "not available in web mode" } }, 403);
  }
```

In acelasi pas, corecteaza comentariul stale existent de deasupra check-ului de loopback (referentiaza `/api/v1/admin/health`, ruta care NU exista in `admin.ts` — vezi triaj F04). Inlocuieste fraza `In mod web, admin loggat acceseaza via /api/v1/admin/health (mount-uit dupa ownerContext) — vezi adminRouter.` cu:

```ts
  // In mod web ruta intoarce 403 neconditionat (gate-ul de mai sus); un
  // endpoint admin-only pentru telemetrie (ex. /api/v1/admin/health) e
  // follow-up — vezi triajul F04 din planul 2026-07-09.
```

- [ ] **Step 4: Ruleaza suita index**

Run: `cd backend && npx vitest run src/index.test.ts`
Expected: PASS — inclusiv cele doua teste emailConfigured mutate pe desktop.

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/index.ts backend/src/index.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/index.ts backend/src/index.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 5: /health/detail 403 neconditionat in web mode
```

---

### Task 6: Bug 6 (MEDIU) — ownerId obligatoriu in avizRepository + searchRepository

**Files:**
- Modify: `backend/src/db/avizRepository.ts:343,352,532,545,560,585,607` (7 semnaturi)
- Modify: `backend/src/db/searchRepository.ts:68,77` (2 semnaturi)
- Modify: call site-urile pe care `tsc` le scoate la iveala (asteptat: prewarm-ul de boot din `index.ts` si eventuale teste)

**Interfaces:**
- Consumes: nimic nou.
- Produces: semnaturi cu `ownerId: string` obligatoriu pentru `getAvizById`, `getAvizByIdentificator`, `deleteAviz`, `deleteAllAvize`, `deleteAvizeByIds`, `getAvizStats`, `getAvizeByIds`, `updateSearchTotal`, `deleteSearch` — omisiunea devine eroare de compilare (protectie tenant isolation).

- [ ] **Step 1: Elimina default-urile**

In ambele fisiere, inlocuieste in cele 9 semnaturi `ownerId = "local"` cu `ownerId: string`. Exemplu:

```ts
export function deleteAviz(id: number, ownerId: string): boolean {
```

Nu atinge corpurile functiilor.

- [ ] **Step 2: Ruleaza tsc si repara call site-urile**

Run: `npx tsc --noEmit -p backend/tsconfig.json`
Expected: erori DOAR la call site-uri care omiteau parametrul. Repara-le astfel:
- Rute HTTP: paseaza `getOwnerId(c)` (conventia existenta; in web mode arunca daca ownerId lipseste).
- Prewarm/boot desktop-only (ex. `getAvizStats` in `index.ts`): paseaza explicit `"local"`.
- Teste: paseaza explicit `"local"` (sau ownerId-ul fixture-ului).

Cauta si audit-ul complet: `grep -rn "getAvizById\|getAvizByIdentificator\|deleteAviz\|deleteAllAvize\|deleteAvizeByIds\|getAvizStats\|getAvizeByIds\|updateSearchTotal\|deleteSearch" backend/src --include="*.ts"` — fiecare apel trebuie sa aiba ownerId explicit.

- [ ] **Step 3: Ruleaza testele backend**

Run: `npm test --workspace=backend`
Expected: PASS — comportament runtime neschimbat (rutele pasau deja ownerId-ul din context).

- [ ] **Step 4: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/db/avizRepository.ts backend/src/db/searchRepository.ts <call-site-urile atinse>
npx tsc --noEmit -p backend/tsconfig.json
git add -A backend/src
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 6: ownerId obligatoriu in avizRepository + searchRepository
```

---

### Task 7: Bug 7 (MEDIU) — scheduler: guard pe finalize() + durationMs real in catch

**Files:**
- Modify: `backend/src/services/monitoring/scheduler.ts` (`runOne` — tranzactia de finalize, azi liniile 546–567; `runJobNow` — catch-ul, azi liniile 267 si 303–333)
- Test: fisierul de teste existent al schedulerului (`backend/src/services/monitoring/scheduler.test.ts` sau echivalentul gasit cu `Glob backend/src/services/monitoring/*.test.ts` — foloseste harness-ul lui existent: FakeClock, DB temporar, runners stub)

**Interfaces:**
- Consumes: `finalize(runId, input): boolean` din `monitoringRunsRepository.ts` (return-ul exista deja — `info.changes > 0` pe `WHERE status = 'running'`).
- Produces: `runOne` nu mai apeleaza `applyJobOutcome` pe run deja terminal si logheaza `monitoring.finalize_noop`; catch-ul din `runJobNow` scrie durata reala.

- [ ] **Step 1: Scrie testul failing**

In fisierul de teste al schedulerului, cu harness-ul existent (job + runner stub). Scenariul: runner-ul finalizeaza SINGUR run-ul (simuleaza recovery/abort concurent) inainte sa returneze:

```ts
it("run deja terminal la finalize: fail_streak neschimbat, fara alerta, log finalize_noop (Bug 7 v2.42.1)", async () => {
  const warnSpy = vi.spyOn(console, "warn").mockImplementation(() => {});
  // Runner stub care saboteaza: finalizeaza run-ul ca 'aborted' INAINTE ca
  // scheduler-ul sa apuce sa-l finalizeze el (simuleaza recovery concurent).
  const runner = {
    run: async ({ runId }: { runId: number }) => {
      finalize(runId, {
        status: "aborted",
        endedAt: clock.now().toISOString(),
        durationMs: 1,
        alertsCreated: 0,
        alertsPatched: 0,
      });
      return { status: "error", errorCode: "X", errorMessage: "boom" } as const;
    },
  };
  // ... porneste scheduler-ul cu runner-ul stub pe kind-ul jobului (helperii
  // existenti ai fisierului), ruleaza jobul, asteapta terminarea.

  const jobAfter = getJobById(job.id); // helperul de citire al fisierului
  expect(jobAfter.fail_streak).toBe(0); // NU s-a dublu-aplicat outcome-ul
  // niciun alert source_error emis:
  expect(countAlertsForJob(job.id)).toBe(0);
  // log structurat:
  expect(warnSpy.mock.calls.some((args) => String(args[0]).includes("monitoring.finalize_noop"))).toBe(true);
});
```

(Adapteaza numele helperilor la cele reale din fisier — proprietatile verificate sunt cele trei asserts.)

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/services/monitoring/scheduler.test.ts -t "Bug 7"`
Expected: FAIL — azi `applyJobOutcome` ruleaza si pe run deja terminal → `fail_streak` devine 1.

- [ ] **Step 3: Implementeaza guard-ul in `runOne`**

In tranzactia din `runOne`, inlocuieste apelul `finalize(runId, {...});` + blocul `if (outcome.status !== "aborted") {...}` cu:

```ts
          const didFinalize = finalize(runId, {
            status: outcome.status,
            endedAt: endIso,
            durationMs,
            httpStatus: outcome.httpStatus,
            errorCode: outcome.errorCode,
            errorMessage: outcome.errorMessage,
            alertsCreated: outcome.alertsCreated ?? 0,
            alertsPatched: outcome.alertsPatched ?? 0,
          });
          if (!didFinalize) {
            // Bug 7 (v2.42.1): run deja terminal (finalizat concurent /
            // recovery) — skip applyJobOutcome ca sa nu dublu-aplicam
            // fail_streak/next_run_at si sa nu emitem alerte source_error
            // false. Consecinta asumata: pe ramura non-duplicate, next_run_at
            // ramane in trecut si urmatorul tick re-claimeaza jobul imediat.
            // Acceptabil sub single-writer (single-instance lock desktop /
            // single-replica server).
            console.warn(
              JSON.stringify({ action: "monitoring.finalize_noop", job_id: job.id, run_id: runId, ts: endIso })
            );
            return;
          }

          // 'aborted' is graceful drain, NOT a retry-able failure. Leave job
          // state (fail_streak, next_run_at, last_*) untouched so the next boot
          // resumes the job at its existing schedule. Counting it would inflate
          // fail_streak on every clean shutdown and trip spurious source_error
          // alerts on healthy jobs.
          if (outcome.status !== "aborted") {
            sourceErrorResult = this.applyJobOutcome(job, runId, outcome, nowIso);
          }
```

(`return`-ul iese din closure-ul tranzactiei; `sourceErrorResult` ramane `null`, deci dispatch-ul de email de dupa tranzactie e sarit — corect.)

- [ ] **Step 4: durationMs real in catch-ul din `runJobNow`**

Dupa `const nowIso = this.opts.clock.now().toISOString();` (linia ~267) adauga:

```ts
    const startMs = this.opts.clock.now().getTime();
```

In catch-ul `this.runOne(job, runId, nowIso).catch((err) => {...})`, inlocuieste `durationMs: 0,` cu:

```ts
          durationMs: this.opts.clock.now().getTime() - startMs,
```

- [ ] **Step 5: Ruleaza suita monitoring**

Run: `cd backend && npx vitest run src/services/monitoring/`
Expected: PASS.

- [ ] **Step 6: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/services/monitoring/scheduler.ts backend/src/services/monitoring/scheduler.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/services/monitoring/
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 7: guard pe finalize() + durationMs real in catch-ul runJobNow
```

---

### Task 8: Bugurile desktop 8, 9, 10 — electron/main.js + ecou bootNonce in /health

**Files:**
- Modify: `electron/main.js` (linia 122 `IS_DEV`; liniile 275–317 IPC; linia 83 timeout; liniile 215–266 startBackend/health-check; require crypto la top)
- Modify: `backend/src/index.ts` (`publicHealthHandler`, azi liniile 289–294)
- Test: `backend/src/index.test.ts` (ecou bootNonce); pentru `main.js` nu exista harness de teste — verificare prin biome + smoke manual `npm run electron:dev`

**Interfaces:**
- Consumes: `app.isPackaged` (Electron), `randomUUID` din `node:crypto`, `mainWindow` (variabila modul existenta).
- Produces: `/health` include campul `bootNonce` DOAR cand env-ul `LEGAL_DASHBOARD_BOOT_NONCE` e setat (omis complet in server/web mode); `isTrustedIpcSender(event)` in `main.js`.

- [ ] **Step 1: Test failing pentru ecoul bootNonce (backend)**

In `index.test.ts`:

```ts
  it("/health ecoul bootNonce doar cand LEGAL_DASHBOARD_BOOT_NONCE e setat (Bug 9)", { timeout: 20_000 }, async () => {
    const port = randomPort();
    await importFreshIndex({
      LEGAL_DASHBOARD_PORT: String(port),
      LEGAL_DASHBOARD_DB_PATH: await makeTmpDb(),
      LEGAL_DASHBOARD_AUTH_MODE: "desktop",
      LEGAL_DASHBOARD_BOOT_NONCE: "nonce-test-123",
    });
    const health = await waitForHealth(port);
    expect(health.status).toBe(200);
    const body = (await health.json()) as { bootNonce?: string };
    expect(body.bootNonce).toBe("nonce-test-123");
  });
```

Si in testul existent `"/health public response strips operational telemetry (F15 audit hardening)"` adauga assertul de absenta:

```ts
    expect(body.bootNonce).toBeUndefined();
```

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/index.test.ts -t "bootNonce"`
Expected: FAIL — `/health` nu emite campul azi.

- [ ] **Step 3: Implementeaza ecoul in `publicHealthHandler`**

```ts
function publicHealthHandler(c: Context): Response {
  if (!ready) {
    return c.json({ status: "starting", service: "Legal Dashboard API" }, 503);
  }
  // Bug 9 (v2.42.1): health-check-ul de boot din Electron verifica un nonce
  // generat de main process (port-squat protection). Campul e emis DOAR cand
  // env-ul e setat de main inainte de require-ul backend-ului in-proc — in
  // server/web mode e omis complet.
  const bootNonce = process.env.LEGAL_DASHBOARD_BOOT_NONCE;
  return c.json({ status: "ok", service: "Legal Dashboard API", ...(bootNonce ? { bootNonce } : {}) });
}
```

Run: `cd backend && npx vitest run src/index.test.ts` → PASS.

- [ ] **Step 4: Bug 8 — IS_DEV din app.isPackaged**

`electron/main.js` linia 122:

```js
// Bug 8 (v2.42.1): NODE_ENV se seteaza pe "production" abia in startBackend(),
// DUPA ce modulul s-a evaluat — build-urile instalate ramaneau cu DevTools +
// meniul de debug active. app.isPackaged e adevarul direct al packagerului.
const IS_DEV = !app.isPackaged;
```

- [ ] **Step 5: Bug 9 — validare sender IPC**

Deasupra `registerSafeStorageIpc()` (linia ~275):

```js
// Valideaza ca IPC-ul vine din webContents-ul ferestrei principale. NU
// blocheaza codul injectat din consola DevTools (acela ruleaza in acelasi
// webContents si trece check-ul) — mitigarea reala pentru consola e
// devTools: IS_DEV. Check-ul opreste webContents secundare (webview, popup).
function isTrustedIpcSender(event) {
  return Boolean(mainWindow) && !mainWindow.isDestroyed() && event.sender === mainWindow.webContents;
}
```

Apoi in fiecare handler (`safeStorage:available`, `safeStorage:encrypt`, `safeStorage:decrypt`, `window:setTheme`) redenumeste `_event` in `event` si adauga prima linie:

```js
  ipcMain.handle("safeStorage:available", (event) => {
    if (!isTrustedIpcSender(event)) return false;
    return safeStorage.isEncryptionAvailable();
  });

  ipcMain.handle("safeStorage:encrypt", (event, plaintext) => {
    if (!isTrustedIpcSender(event)) return null;
    // ... corpul existent neschimbat
  });

  ipcMain.handle("safeStorage:decrypt", (event, ciphertextB64) => {
    if (!isTrustedIpcSender(event)) return null;
    // ... corpul existent neschimbat
  });

  ipcMain.handle("window:setTheme", (event, theme) => {
    if (!isTrustedIpcSender(event)) return;
    // ... corpul existent neschimbat
  });
```

- [ ] **Step 6: Bug 9 — nonce la health-check-ul de boot**

La top-ul `main.js`, linga celelalte require-uri:

```js
const { randomUUID } = require("node:crypto");
```

In `startBackend()`, inainte de `require(path.join(__dirname, "..", "dist-backend", "index.cjs"));`:

```js
  // Bug 9 (v2.42.1): nonce generat de main, ecou in /health — health-check-ul
  // de boot nu mai poate fi pacalit de un alt proces care a ocupat portul
  // (port squat) si raspunde cu service-ul asteptat.
  const bootNonce = randomUUID();
  process.env.LEGAL_DASHBOARD_BOOT_NONCE = bootNonce;
```

Si in check-ul de identitate (linia ~247), inlocuieste conditia:

```js
          if (body && body.service === "Legal Dashboard API" && body.bootNonce === bootNonce) {
```

(`bootNonce` e in scope: declara-l inainte de `return new Promise(...)` din aceeasi functie.)

- [ ] **Step 7: Bug 10 — shutdown 40s**

Linia 83 + comentariul de deasupra (liniile 79–81):

```js
// Capul exterior TREBUIE sa fie peste bugetul intern de drain al backend-ului
// (SHUTDOWN_DRAIN_MS 30s + drainEmailDispatches 5s) — altfel orice quit in
// timpul unui tick de monitoring taie drain-ul si arunca outcome-ul run-ului
// in zbor (Bug 10 v2.42.2). Drain-ul se rezolva in milisecunde cand nu e nimic
// in zbor, deci quit-ul normal ramane instant; 40s e doar plafonul patologic.
let backendShutdownStarted = false;
const BACKEND_SHUTDOWN_TIMEOUT_MS = 40_000;
```

- [ ] **Step 8: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write electron/main.js backend/src/index.ts backend/src/index.test.ts
npx tsc --noEmit -p backend/tsconfig.json
cd backend && npx vitest run src/index.test.ts && cd ..
git add electron/main.js backend/src/index.ts backend/src/index.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Bug 8-10: IS_DEV din app.isPackaged, validare sender IPC, boot nonce, shutdown 40s
```

Smoke manual (inainte de push, poate fi facut la Task 9): `npm run electron:dev` — app-ul porneste, health-check-ul trece (nonce-ul se potriveste), quit normal instant.

---

### Task 8b: Mitigare F06 (audit extern) — catch pe scrierile SSE fire-and-forget din RNPM

**Files:**
- Modify: `backend/src/routes/rnpm.ts:487-491` (helper `send` bulk), `:605-609` (helper `send` split), `:633-636` (`started` split)

**Interfaces:**
- Consumes: nimic nou.
- Produces: aceleasi fluxuri SSE, dar un `writeSSE` rejectat (client disconectat mid-stream) se logheaza in loc sa devina unhandled rejection — care in server/web mode inseamna `process.exit(1)` (`index.ts:167-172`) si doborarea intregului proces multi-tenant.

**De ce fara test failing (exceptie asumata de la TDD):** reproducerea unui reject de `writeSSE` mid-stream prin ruta reala cere sabotarea stream-ului Hono in plin zbor — cost disproportionat pentru un catch defensiv fara schimbare de comportament pe happy path. Fixul complet (coada seriala awaited, cu teste de abort/ordering/disconnect) e in follow-up P1; acest task inchide DOAR vectorul de proces-kill.

- [ ] **Step 1: Helper-ul `send` din bulk (linia ~487)**

```ts
    const send = (p: BulkProgress) =>
      stream
        .writeSSE({
          event: "progress",
          data: JSON.stringify(p),
        })
        .catch((err) => {
          // F06 (audit 2026-07-09): un reject aici (client deconectat) ar deveni
          // unhandledRejection => process.exit(1) in server mode. Log si continua;
          // fluxul se opreste oricum prin abort/timeout.
          console.error("[rnpm] bulk progress writeSSE failed (client disconnected?)", err);
        });
```

- [ ] **Step 2: Helper-ul `send` din split (linia ~605)**

```ts
    const send = (p: SplitSearchProgress) =>
      stream
        .writeSSE({
          event: "progress",
          data: JSON.stringify(p),
        })
        .catch((err) => {
          console.error("[rnpm] split progress writeSSE failed (client disconnected?)", err);
        });
```

- [ ] **Step 3: Evenimentul `started` din split (linia ~633)**

```ts
          void stream
            .writeSSE({
              event: "started",
              data: JSON.stringify({ searchId: sid }),
            })
            .catch((err) => {
              console.error("[rnpm] split started writeSSE failed (client disconnected?)", err);
            });
```

- [ ] **Step 4: Verifica suita rnpm existenta**

Run: `cd backend && npx vitest run src/routes/rnpm.test.ts src/services/rnpmSearchService.test.ts`
Expected: PASS — happy path neschimbat (catch-ul e no-op cand writeSSE rezolva).

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/routes/rnpm.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/routes/rnpm.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# F06: catch pe scrierile SSE fire-and-forget din rnpm (anti proces-kill)
```

Nota paritate: acelasi fix de 3 call site-uri se aplica si pe repo-ul GitHub (sursele raman sincronizate) — de facut la urmatorul sync, nu din acest branch.

---

### Task 8c: Bug import utilizatori — emailul din hyperlink mailto (Outlook) se pierde in favoarea textului afisat

**Files:**
- Modify: `backend/src/services/userImport.ts:49-65` (`cellToString`) si `:101-105` (call site-ul din `eachRow`)
- Test: `backend/src/services/userImport.test.ts`

**Interfaces:**
- Consumes: helper-ul de test `xlsxOf` existent (semnatura se largeste de la `(string | number | null)[][]` la `ExcelJS.CellValue[][]` — compatibil cu apelurile existente).
- Produces: `cellToString(value, preferMailto = false)` — cu `preferMailto: true` DOAR pe coloana 1 (email); restul coloanelor pastreaza comportamentul actual (textul afisat castiga).

**Context:** bug confirmat in codul nou al sprintului v2.42.0 (vezi Decizia 9). Ramura `mailto:` din `cellToString` e moarta pentru ca `text` exista intotdeauna pe celulele-hyperlink si e verificat primul. Fixul naiv "muta hyperlink inaintea text" ar strica coloana "Nume afisat" (nume cu link → se importa URL-ul), deci preferinta pentru `mailto:` e explicita si limitata la coloana de email.

- [ ] **Step 1: Largeste helper-ul de test si scrie testele failing**

In `userImport.test.ts`, schimba semnatura helper-ului:

```ts
async function xlsxOf(rows: ExcelJS.CellValue[][]): Promise<Buffer> {
```

(corpul ramane identic; apelurile existente cu string-uri raman valide). Apoi adauga:

```ts
describe("celule hyperlink (paste din Outlook)", () => {
  it("emailul se ia din hyperlink-ul mailto cand textul afisat difera de adresa", async () => {
    const buf = await xlsxOf([
      ["Email", "Nume afisat", "Rol"],
      [{ text: "Ion Popescu", hyperlink: "mailto:ion@firma.ro" }, "Ion Popescu", "Utilizator"],
    ]);
    const res = await parseUserImport(buf);
    expect(res.ok).toBe(true);
    if (!res.ok) return;
    expect(res.issues).toHaveLength(0);
    expect(res.rows).toEqual([{ rowNumber: 2, email: "ion@firma.ro", displayName: "Ion Popescu", role: "user" }]);
  });

  it("parametrii mailto (?subject=...) se taie din adresa", async () => {
    const buf = await xlsxOf([
      ["Email", "Nume afisat", "Rol"],
      [{ text: "Ion Popescu", hyperlink: "mailto:ion@firma.ro?subject=Salut" }, "Ion", ""],
    ]);
    const res = await parseUserImport(buf);
    expect(res.ok).toBe(true);
    if (!res.ok) return;
    expect(res.rows[0]?.email).toBe("ion@firma.ro");
  });

  it("REGRESIE: numele afisat cu hyperlink NU se inlocuieste cu URL-ul", async () => {
    const buf = await xlsxOf([
      ["Email", "Nume afisat", "Rol"],
      ["ion@firma.ro", { text: "Ion Popescu", hyperlink: "https://firma.ro/ion" }, ""],
    ]);
    const res = await parseUserImport(buf);
    expect(res.ok).toBe(true);
    if (!res.ok) return;
    expect(res.rows[0]?.displayName).toBe("Ion Popescu");
  });

  it("email cu hyperlink NON-mailto pastreaza textul afisat", async () => {
    const buf = await xlsxOf([
      ["Email", "Nume afisat", "Rol"],
      [{ text: "ion@firma.ro", hyperlink: "https://firma.ro" }, "Ion", ""],
    ]);
    const res = await parseUserImport(buf);
    expect(res.ok).toBe(true);
    if (!res.ok) return;
    expect(res.rows[0]?.email).toBe("ion@firma.ro");
  });
});
```

- [ ] **Step 2: Ruleaza si confirma FAIL**

Run: `cd backend && npx vitest run src/services/userImport.test.ts -t "hyperlink"`
Expected: primul test FAIL (randul pica la validare cu issue "Email invalid" — textul afisat "Ion Popescu" nu e email); testele 3 si 4 trec deja (sunt garduri de regresie pentru fix).

- [ ] **Step 3: Implementeaza fixul in `cellToString`**

```ts
// Celulele exceljs pot fi string, numar, data, richText, hyperlink (mailto pe
// emailuri lipite din Outlook) sau formula cu result — normalizam totul la text.
// `preferMailto` (doar coloana de email): celulele-hyperlink au INTOTDEAUNA
// { text, hyperlink }, iar textul afisat poate diferi de adresa (ex. "Ion
// Popescu" -> mailto:ion@firma.ro) — adresa reala e in hyperlink.
function cellToString(value: ExcelJS.CellValue, preferMailto = false): string {
  if (value === null || value === undefined) return "";
  if (typeof value === "string") return value;
  if (typeof value === "number" || typeof value === "boolean") return String(value);
  if (value instanceof Date) return value.toISOString();
  if (typeof value === "object") {
    if (
      preferMailto &&
      "hyperlink" in value &&
      typeof value.hyperlink === "string" &&
      /^mailto:/i.test(value.hyperlink)
    ) {
      return value.hyperlink.replace(/^mailto:/i, "").split("?")[0];
    }
    if ("richText" in value && Array.isArray(value.richText)) {
      return value.richText.map((part) => part.text ?? "").join("");
    }
    if ("text" in value && value.text !== undefined) return cellToString(value.text as ExcelJS.CellValue);
    if ("result" in value && value.result !== undefined) return cellToString(value.result as ExcelJS.CellValue);
    if ("hyperlink" in value && typeof value.hyperlink === "string") {
      return value.hyperlink.replace(/^mailto:/i, "");
    }
  }
  return "";
}
```

Si call site-ul din `eachRow` (linia ~104):

```ts
      cells.push(cellToString(row.getCell(col).value, col === 1).trim());
```

- [ ] **Step 4: Ruleaza suita fisierului**

Run: `cd backend && npx vitest run src/services/userImport.test.ts`
Expected: PASS pe tot (inclusiv testele existente — comportamentul pe string/richText/formula/coloane non-email e neschimbat).

- [ ] **Step 5: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/services/userImport.ts backend/src/services/userImport.test.ts
npx tsc --noEmit -p backend/tsconfig.json
git add backend/src/services/userImport.ts backend/src/services/userImport.test.ts
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Import useri: emailul din hyperlink-ul mailto (paste Outlook), ramura era de neatins
```

Nota paritate: `userImport.ts` exista si pe GitHub (sprintul v2.42.0 e sincronizat) — acelasi fix se aplica acolo la urmatorul sync.

---

### Task 8d: Migrare modele OpenAI — GPT-5.4 → GPT-5.6 Sol / Terra / Luna

**Files:**
- Modify: `backend/src/services/ai.ts` (map `AI_MODELS` liniile ~25-27, `JUDGE_MODELS` linia ~34, `OPENROUTER_MODEL_MAP` liniile ~40-42, comentariul de la ~543 care citeaza "gpt-5.4 reasoning models")
- Modify: `backend/src/services/aiUsage.ts:48-52` (pricing openai) si `:63-65` (pricing openrouter) — ADAUGA intrarile 5.6, PASTREAZA intrarile 5.4
- Modify: `frontend/src/components/dosare-ai-config.ts:23-31` (`AI_MODELS`) si `:44-60` (`JUDGE_MODELS_LIST`)
- Modify: `frontend/src/hooks/useDosareAi.ts:75` (default `multiAnalysts`)
- Modify: `frontend/src/pages/manual-content.tsx` + `frontend/src/lib/export-manual.ts` (mentiunile GPT-5.4 din manualul utilizatorului)
- Test: `frontend/src/components/dosare-ai-config.test.ts`, `backend/src/services/ai.openrouter.test.ts`, `backend/src/routes/ai.contract.test.ts` + triajul celorlalte fixtures

**Interfaces:**
- Consumes: slug-urile si pricing-ul VERIFICATE din Decizia 7 (native `gpt-5.6-sol/-terra/-luna`; OpenRouter `openai/gpt-5.6-*`; Sol $5/$30, Terra $2.50/$15, Luna $1/$6 per 1M). Disponibilitatea: DECISA — swap direct, tenantul foloseste chei OpenRouter unde modelele sunt live (vezi Decizia 7). **Nicio intrebare deschisa — task-ul e gata de executie.**
- Produces: chei interne noi `gpt-5.6-sol` (Premium), `gpt-5.6-terra` (Echilibrat), `gpt-5.6-luna` (Rapid); cheile `gpt-5.4*` dispar din cataloage (backend raspunde 400 "Model necunoscut." la ele) dar RAMAN in tabelele de pricing.

- [ ] **Step 1: Actualizeaza testele de catalog (failing first)**

In `dosare-ai-config.test.ts`: inlocuieste asteptarile `gpt-5.4-nano`/`-mini`/`gpt-5.4` cu `gpt-5.6-luna`/`-terra`/`-sol` si etichetele cu "5.6 Luna" / "5.6 Terra" / "GPT-5.6 Sol". In `ai.openrouter.test.ts`: slug-urile asteptate devin `openai/gpt-5.6-*`. In `ai.contract.test.ts`: adauga un test explicit ca vechea cheie e respinsa curat:

```ts
  it("POST /api/ai/analyze cu cheia veche gpt-5.4 returneaza UNKNOWN_MODEL 400 (migrare 5.6)", async () => {
    // copiaza structura testului existent "model necunoscut" (linia ~50),
    // cu body-ul { model: "gpt-5.4", ... } — dupa migrare cheia nu mai exista.
  });
```

Run: `cd backend && npx vitest run src/services/ai.openrouter.test.ts src/routes/ai.contract.test.ts && cd ../frontend && npx vitest run src/components/dosare-ai-config.test.ts --run`
Expected: FAIL pe asteptarile 5.6 (cataloagele inca au 5.4).

- [ ] **Step 2: Backend — cataloage + judge**

In `backend/src/services/ai.ts`:

```ts
  // OpenAI — v2.42.x: familia GPT-5.6 inlocuieste 5.4 (SOL=premium,
  // Terra=echilibrat, Luna=rapid). Chei interne noi, versionate; cheile 5.4
  // nu mai exista in catalog — requesturile cu ele primesc 400 Model necunoscut.
  "gpt-5.6-luna": { provider: "openai", modelId: "gpt-5.6-luna" },
  "gpt-5.6-terra": { provider: "openai", modelId: "gpt-5.6-terra" },
  "gpt-5.6-sol": { provider: "openai", modelId: "gpt-5.6-sol" },
```

`JUDGE_MODELS` devine:

```ts
export const JUDGE_MODELS = ["claude-opus", "gpt-5.6-sol", "gemini-pro-3"];
```

`OPENROUTER_MODEL_MAP` — inlocuieste cele 3 intrari openai:

```ts
  "gpt-5.6-luna": "openai/gpt-5.6-luna",
  "gpt-5.6-terra": "openai/gpt-5.6-terra",
  "gpt-5.6-sol": "openai/gpt-5.6-sol",
```

Actualizeaza si comentariul de la linia ~543 ("configured gpt-5.4 reasoning models" → "configured gpt-5.6 reasoning models").

- [ ] **Step 3: Backend — pricing (ADAUGA, nu inlocui)**

In `aiUsage.ts`, sub intrarile openai 5.4 existente (care raman — retry-uri/cozi in zbor, pattern-ul Sonnet 5 de la liniile 41-45):

```ts
    // v2.42.x: familia 5.6 — preturi de PREVIEW verificate 2026-07-09
    // (OpenRouter live catalog + surse pricing); de reverificat la GA.
    "gpt-5.6-luna": { inputUsdPerMillion: 1, outputUsdPerMillion: 6 },
    "gpt-5.6-terra": { inputUsdPerMillion: 2.5, outputUsdPerMillion: 15 },
    "gpt-5.6-sol": { inputUsdPerMillion: 5, outputUsdPerMillion: 30 },
```

si echivalentele openrouter (sub `openai/gpt-5.4*`, care raman; pe OpenRouter pricing-ul 5.6 e identic cu cel nativ — verificat in catalogul live):

```ts
    "openai/gpt-5.6-luna": { inputUsdPerMillion: 1, outputUsdPerMillion: 6 },
    "openai/gpt-5.6-terra": { inputUsdPerMillion: 2.5, outputUsdPerMillion: 15 },
    "openai/gpt-5.6-sol": { inputUsdPerMillion: 5, outputUsdPerMillion: 30 },
```

- [ ] **Step 4: Frontend — catalog, judge list, default analysts, manual**

`dosare-ai-config.ts`:

```ts
  // OpenAI
  { key: "gpt-5.6-luna", label: "5.6 Luna", provider: "openai", desc: "Rapid", color: "emerald" },
  {
    key: "gpt-5.6-terra",
    label: "5.6 Terra",
    provider: "openai",
    desc: "Echilibrat",
    color: "emerald",
  },
  { key: "gpt-5.6-sol", label: "GPT-5.6 Sol", provider: "openai", desc: "Premium", color: "emerald" },
```

In `JUDGE_MODELS_LIST`: `{ key: "gpt-5.6-sol", label: "GPT-5.6 Sol", provider: "openai", color: "emerald", desc: "Premium" }`.

In `useDosareAi.ts:75`: `["claude-sonnet", "gpt-5.6-terra"]`.

In `manual-content.tsx` + `export-manual.ts`: `Grep -n "5\.4"` si inlocuieste mentiunile de modele GPT-5.4 cu echivalentul 5.6 (atentie: NU atinge referintele "(5.4)" care sunt numere de pasi de plan in comentarii, ex. `adminApi.ts:77`).

- [ ] **Step 5: Triajul restului de fixtures**

`Grep -rn "gpt-5.4" backend/src frontend/src` — pentru fiecare hit ramas decide: (a) fixture care asserteaza CATALOGUL (trebuie migrat la 5.6); (b) string arbitrar de model in teste de usage/repository/audit (RAMANE — testeaza stocarea, nu catalogul); (c) istoric in `changelog-entries.tsx` (NU se atinge). `Dashboard.tsx` si testele de usage intra tipic la (b).

- [ ] **Step 6: Ruleaza suitele complete**

Run: `npm test --workspace=backend && cd frontend && npx vitest run`
Expected: PASS.

- [ ] **Step 7: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write backend/src/services/ai.ts backend/src/services/aiUsage.ts frontend/src/components/dosare-ai-config.ts frontend/src/hooks/useDosareAi.ts frontend/src/pages/manual-content.tsx frontend/src/lib/export-manual.ts <testele atinse>
npx tsc --noEmit -p backend/tsconfig.json
cd frontend && npx tsc --noEmit && cd ..
git add -A backend/src frontend/src
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# AI: migrare modele OpenAI la GPT-5.6 Sol/Terra/Luna, pricing 5.4 pastrat
```

Nota paritate: aceeasi migrare se aplica pe GitHub la urmatorul sync (cataloagele de modele sunt surse comune).

---

### Task 8e: Changelog — extinde entry-ul v2.42.0 (in-app + CHANGELOG.md, FARA bump)

**Files:**
- Modify: `frontend/src/data/changelog-entries.tsx` (entry-ul v2.42.0 existent)
- Modify: `CHANGELOG.md` (sectiunea v2.42.0 existenta)

**Interfaces:**
- Consumes: decizia userului (Decizia 8b): FARA bump de versiune — branch-ul este release-ul v2.42.0 pe GitLab, deci fixurile se documenteaza in entry-ul lui.
- Produces: entry v2.42.0 extins cu fixurile si migrarea GPT-5.6; `package.json` NEATINS.

- [ ] **Step 1: Extinde entry-ul v2.42.0 din `changelog-entries.tsx`**

Citeste structura entry-urilor existente si adauga la entry-ul v2.42.0, respectand exact stilul fisierului (romana fara diacritice, componentele/tag-urile folosite de entry-urile vecine), o sectiune de tip "Hardening si fixuri post-review" cu punctele (formulate pentru utilizatori, nu pentru dezvoltatori):

- Limita globala de 1MB pe request-urile API (protectie memorie server); exporturile mari si importurile de liste raman neafectate.
- Limitarea de trafic corectata: cautarile AI multiple se contorizeaza corect pe ambele rute, iar utilizatorii legitimi din spatele aceluiasi proxy nu mai pot fi blocati din greseala.
- Pagina de stare detaliata a serverului nu mai e expusa in modul web.
- Protectie suplimentara la izolarea datelor intre utilizatori (parametru obligatoriu la nivel de cod).
- Monitorizare: rezultatele nu se mai aplica de doua ori pe un job deja finalizat; durata reala inregistrata la erori.
- Desktop: DevTools dezactivat in build-urile instalate, validare IPC, verificare de identitate la pornirea backend-ului, inchidere care asteapta drain-ul monitorizarii.
- Cautarile RNPM in masa nu mai pot opri serverul daca clientul inchide pagina in timpul procesarii.
- Importul de utilizatori citeste corect emailurile lipite din Outlook ca link.
- Modele AI actualizate: GPT-5.6 Sol (premium), Terra (echilibrat), Luna (rapid) inlocuiesc familia GPT-5.4.

- [ ] **Step 2: Extinde sectiunea v2.42.0 din `CHANGELOG.md`**

Adauga subsectiunea "Hardening si fixuri post-review (2026-07-09, pre-merge)" cu aceleasi puncte, formulate tehnic (fisier + bug), respectand conventia Markdown din CLAUDE.md (fara linii care incep cu `-` in interiorul paragrafelor bold, bold pe o singura linie).

- [ ] **Step 3: Gates + stage (fara commit — commit unic in Task 9)**

```bash
npx biome check --write frontend/src/data/changelog-entries.tsx
cd frontend && npx tsc --noEmit && npx vitest run && cd ..
git add frontend/src/data/changelog-entries.tsx CHANGELOG.md
# FARA commit (regula: commit unic in Task 9). Nota pentru commit body:
# Changelog: entry v2.42.0 extins cu hardening + fixuri + migrarea GPT-5.6
```

---

### Task 9: Gate final complet + audit advers + smoke + commit unic + push (cu confirmarea userului)

**Files:** niciunul nou — verificare end-to-end.

- [ ] **Step 1: Suita completa**

```bash
npm run check
npm run build
```

Expected: biome curat, tsc backend + frontend verzi, toate testele backend + frontend trec, build curat. Daca biome a reformatat ceva pe parcurs, re-ruleaza tsc + build si adauga fisierele reformatate la staging — intra in acelasi commit unic (Step 3), NU commit separat.

- [ ] **Step 2: Smoke pe mediul web local**

Porneste `scripts/dev-web-local.ps1` si verifica in browser prin proxy (fara extragere/persistare de JWT-uri in comenzi inline — regula stabilita):
1. Login functioneaza.
2. Un export xlsx real cu multe dosare selectate (>1MB payload) NU primeste 413.
3. Crearea unui token API normal (`POST /api/v1/tokens` cu body mic) functioneaza.
4. `GET /health/detail` intoarce 403.
5. `GET /health` intoarce 200 FARA camp `bootNonce`.

- [ ] **Step 3: AUDIT ADVERS cu review-panel (obligatoriu INAINTE de commit — decizia userului)**

Lanseaza `mcp__review-panel__multi_model_review` pe diff-ul complet al batch-ului (staging-ul curent fata de HEAD, plus contextul branch-ului fata de main). Triaj pe findings:
- CONFIRMATE si in scope (ating fisierele batch-ului): se repara ACUM, cu gate-urile taskului respectiv re-rulate, inainte de commit.
- CONFIRMATE dar out-of-scope: se adauga in sectiunea Follow-up a acestui plan, cu dovada (fisier:linie).
- Neconfirmate/speculative: se noteaza in raportul HTML (Task 10) ca respinse, cu motivul.

Commit-ul unic (Step 4) se face DOAR dupa inchiderea triajului.

- [ ] **Step 4: COMMIT-UL UNIC (regula userului: un singur commit pentru tot batch-ul)**

Verifica intai ca staging-ul contine DOAR fisierele din taskuri (`git status` — niciun fisier strain, niciun doc/plan neintentionat), apoi:

```bash
git commit -m "fix: hardening v2.42.1-2 (body limit, rate-limit, health, scheduler, electron) + fixuri review (SSE rnpm, import mailto) + migrare GPT-5.6

- Bug 1a: plasa globala body limit 1MB pe /api/* cu exceptii exact-match
- Bug 2: release preAuth in try/finally cu guard pe c.finalized
- Bug 3: weight 3x analyze-multi pe ambele mount-uri si pe bucketul per-token
- Bug 4: plafonul se verifica si pe fereastra proaspata, ambele bucket-uri
- Bug 5: /health/detail 403 neconditionat in web mode
- Bug 6: ownerId obligatoriu in avizRepository + searchRepository
- Bug 7: guard pe finalize() + durationMs real in catch-ul runJobNow
- Bug 8-10 (desktop): IS_DEV din app.isPackaged, validare sender IPC, boot nonce, shutdown 40s
- F06 (audit): catch pe scrierile SSE fire-and-forget din rnpm (anti proces-kill)
- Import useri: emailul din hyperlink-ul mailto (paste Outlook)
- AI: migrare modele OpenAI la GPT-5.6 Sol/Terra/Luna, pricing 5.4 pastrat
- Quota: estimari realiste de rezervare AI (single \$0.25, multi \$0.50, fost \$2/\$8) — decizia userului 2026-07-10, politica ~\$5/user; TDD, aliniat la costurile reale din ai_usage
- Changelog: entry v2.42.0 extins cu hardening + fixuri + migrarea GPT-5.6"
```

(Compune body-ul din notele "Nota pentru commit body" lasate de fiecare task — daca un task a fost sarit, scoate linia lui; adauga fixurile iesite din auditul advers, daca au existat.)

- [ ] **Step 5: Push (DOAR cu confirmarea userului)**

```bash
git log --oneline main..feat/v2.42.0-users-settings
git push origin feat/v2.42.0-users-settings
```

Push-ul actualizeaza MR-ul aflat in review; NU se atinge main (Dokploy face deploy de pe main).

- [ ] **Step 6: Post-livrare**

Decizia de versiune e INCHISA (Decizia 8b): fara bump — entry-ul v2.42.0 a fost extins in Task 8e. Daca vreodata se decide totusi un bump ulterior: urmeaza `## Checklist bump de versiune` din CLAUDE.md + capcana `npm version`/biome din Global Constraints.

---

### Task 10: Raport HTML final — bugs rezolvate, efectul lor, implementari noi

**Files:**
- Create: `Legal-Dashboard-v2.42.0-Fixuri-Post-Review.html` (radacina repo-ului, ramane UNTRACKED — ca celelalte rapoarte HTML din radacina; NU intra in commit)

**Interfaces:**
- Consumes: rezultatele reale ale Taskurilor 1–8e (ce s-a implementat efectiv, testele adaugate, numarul lor), triajul auditului advers din Task 9 Step 3.
- Produces: raport HTML self-contained (CSS inline, fara resurse externe, diacritice OK — e continut, nu cod sursa), in romana, in stilul rapoartelor primite (tabel sumar + sectiuni detaliate).

- [ ] **Step 1: Genereaza raportul**

Structura obligatorie:
1. **Header**: titlu, data, branch, scope (v2.42.1→v2.42.2 + audit extern + duel review + migrare AI), mentiunea "un singur commit, push pe branch, main neatins".
2. **Tabel sumar**: # / bug / severitate / fisier / stare (rezolvat/follow-up).
3. **Sectiunea "Ce am rezolvat si ce efect aveau"** — per bug: descrierea pe scurt, EFECTUL daca ramanea (formulat si non-tehnic: ex. "orice admin putea dobori serverul cu un upload mare", "un tab inchis in timpul unei cautari in masa putea opri aplicatia pentru toti"), fixul aplicat, testul care il acopera.
4. **Sectiunea "Implementari noi"**: migrarea GPT-5.6 Sol/Terra/Luna cu pricing-ul verificat, plus orice a iesit din auditul advers.
5. **Sectiunea "Verificari"**: gate-urile rulate (biome/tsc/build/teste + numarul de teste), rezultatul auditului advers cu review-panel (findings confirmate/respinse), smoke-ul web local.
6. **Sectiunea "Ramase pe follow-up"**: lista P0-P2 din plan (F01, F02, F04, F05, F07, F03, F09, F08) cu o fraza de ce nu au intrat in acest batch.

- [ ] **Step 2: Verifica si preda**

Deschide fisierul local (sanity vizual), confirma ca `git status` il arata untracked si ca NU e in staging. Raportul se preda userului ca fisier — nu se comite, nu se publica extern.

---

## Follow-up (NU in acest branch) — remedieri audit extern, dupa merge-ul MR-ului

Ordinea de risc pentru deployment-ul web GitLab (detalii si dovezi in sectiunea de triaj). Fiecare item = branch propriu, aplicat apoi si pe GitHub (paritate surse). Niciunul nu e inceput; planurile de implementare se scriu la momentul respectiv.

**P0 — consistenta datelor:**
1. **F01 — maintenance gate pe restore**: flag setat DOAR in `restoreFromBackupImpl` (nu in tot `withMaintenanceWrite` — daily backup-ul zilnic ar produce altfel 503 global), verificat central in `getDb()`, mapat la 503 + envelope in error handler; teste de concurenta (request in plina fereastra de restore → 503, nu redeschidere).
2. **F02 — esecuri PortalJust explicite**: `Promise.allSettled` in `dosare.ts`/`termene.ts`, campuri noi `partial: boolean` + `failedInstitutions: string[]` in raspuns (extensie aditiva, nu breaking), toate institutiile esuate → eroare 502, frontend afiseaza warning de degradare. Schimbare de contract — coordonat cu UI.

**P1 — degradare controlata:**
3. **F06 complet — coada seriala SSE** in bulk/split (ordering, backpressure, stop la disconnect), cu teste de abort/ordering; mitigarea minima din Task 8b ramane oricum corecta.
4. **F04 — vizibilitate scheduler in web**: `GET /api/v1/admin/health` (admin-only, web) cu `getMonitoringSchedulerStatus()` + `emailConfigured`; repara si referinta din comentariul corectat in Task 5. Optional: retry controlat pe `scheduler.start()`.
5. **F05 — deadline implicit in `apiFetch`**: `AbortSignal.timeout` compus cu signal-ul callerului (`AbortSignal.any`), override explicit pentru export xlsx / AI / SSE.
6. **F07 — offsite upload in afara lock-ului**: muta `runOffsiteBackupHook` dupa eliberarea `withMaintenanceWrite` + tree-kill verificabil (Windows: `taskkill /T /F`; POSIX: `detached: true` + `process.kill(-pid)`).

**P2 — igiena si coverage:**
7. **F03 — fanout SSE post-commit** pe pattern-ul email deja existent (`InsertAlertResult` colectat, notificare dupa commit).
8. **F09 — teardown listeners** in teste (`process.off` in `__legalDashboardShutdown` sau inregistrare unica per proces).
9. **F08 — smoke Electron e2e** — pe repo-ul GitHub (acolo se fac build-urile desktop); irelevant pentru web GitLab.

**Adaugate din auditul advers review-panel (2026-07-09, panel: Opus 4.8 + GPT-5.6 Sol + Kimi K2.7 + GLM-5.2; 4 findings reparate PE LOC in batch — plafon exterior 25MB pe rutele exceptate, flag `completed` in loc de `c.finalized`, gate web pe ecoul bootNonce, decodare mailto RFC 6068):**
10. **OPS (prioritar la deploy): verifica `LEGAL_DASHBOARD_TRUSTED_PROXY_CIDR` pe deployment-ul Dokploy** — fara el, dupa oauth2-proxy toate cererile au IP-ul proxy-ului si bucket-ul preAuth e partajat intre toti userii (60 esecuri/min de la UN client pot bloca autentificarea tuturor). Mecanismul EXISTA in `proxyIp.ts:24` — e configurare, nu cod.
11. **userImport: bugete pe dimensiunea decomprimata / numar celule** — cap-ul de 512KB e pe zip-ul COMPRIMAT; un zip-bomb mic poate decomprima sute de MB, iar timeout-ul de 30s nu opreste parsarea (tradeoff documentat in cod, ruta admin-only; de intarit la primul refactor al importului).
12. **preAuth: release cu generation tracking** — un request care dureaza >60s elibereaza in fereastra URMATOARE (decrementeaza bucket-ul proaspat); de legat release-ul de entry-ul exact consumat.
13. **Electron IPC: valideaza si `event.senderFrame`** (nu doar webContents) — defense-in-depth pentru scenariul ipotetic al unui subframe embedded.
14. **Verifica pricing-ul istoric `openai/gpt-5.4` pe OpenRouter** (2.5 input vs 1.25 nativ) — markup intentionat sau typo istoric; intra doar in tabelul de pricing legacy.

**Respinse cu dovezi (nu se lucreaza):** payloaduri legitime >1MB pe /api/ai sau /api/rnpm (toate limitele per-ruta existente sunt <=512KB); selectii gpt-5.4 persistate care s-ar rupe (selectia de model NU se persista — state React cu default-uri; `ai_usage` e display-only); rute item-level cu payload mare pe resursele exceptate (name-lists are DOAR GET / + POST preview/commit — verificat); bypass chunked al plasei (bodyLimit din Hono 4 numara si stream-urile fara Content-Length); trailing-slash pe exact-match (varianta cade in ramura MAI STRICTA + Hono strict routing); startMs inainte de lock in runJobNow (spec-ul GitHub cere explicit durata de la claim).
