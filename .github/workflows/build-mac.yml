name: Build macOS DMG
run-name: macOS DMG ${{ github.ref_name || github.sha }}

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build-mac:
    runs-on: macos-latest
    timeout-minutes: 45
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build app (backend + frontend)
        run: npm run build

      - name: Package macOS (dmg, x64 + arm64, unsigned)
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --mac --x64 --arm64 --publish never

      - name: Upload DMG artifacts
        uses: actions/upload-artifact@v6
        continue-on-error: true
        with:
          name: legal-dashboard-mac-${{ github.ref_name }}
          path: |
            release/*.dmg
            release/*.dmg.blockmap
          if-no-files-found: error
          retention-days: 14

      - name: Publish DMGs to dev-build prerelease
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: dev-build
          name: "Dev build (macOS)"
          body: |
            Unsigned macOS build produced by workflow_dispatch.
            Commit: ${{ github.sha }}
            Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          files: |
            release/*.dmg
            release/*.dmg.blockmap
          draft: false
          prerelease: true

      - name: Attach DMGs to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          name: "Legal Dashboard ${{ github.ref_name }}"
          body: |
            Legal Dashboard ${{ github.ref_name }} release artifacts.

            Includes:
            - macOS DMG
            - Windows NSIS installer, uploaded by the Windows workflow
            - Docker image published by the Docker workflow to GHCR
          files: |
            release/*.dmg
            release/*.dmg.blockmap
          draft: false
          prerelease: false
