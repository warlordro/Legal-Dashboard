/// <reference types="vite/client" />

// Injected at build time by vite.config.ts (define block) — single source of truth
// for the app version is the root package.json.
declare const __APP_VERSION__: string;
